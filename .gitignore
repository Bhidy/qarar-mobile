node_modules/
.expo/
dist/
web-build/

# Compiled app / build outputs — never commit binaries or logs.
build/
*.aab
*.apk
*.ipa
*.xcarchive/
*.dSYM/
*.jsbundle
*.keystore
*.log

*.jks
*.p8
*.p12
*.key
# OTA (expo-updates) code-signing private key, if ever generated (signing needs
# the EAS Enterprise plan; not used today). Kept ignored as a safety net.
keys/
*.mobileprovision
*.orig.*
.env
.env.local
.DS_Store
ios/
android/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# EAS local credentials — contains the Android keystore path + keystore/key
# PASSWORDS in plaintext. MUST never be committed.
credentials.json

# Editor/agent local config (machine-specific paths)
.claude/
